---
import { Icon } from 'astro-icon/components'
import MainContainer from '../containers/main-container.astro'
import { Image } from 'astro:assets'
import HeroImage from '../assets/hero-image.webp'
import { SlideInFromBottom } from '../components/motion/slide-in'
---

<div class="bg-primary text-background overflow-hidden">
  <MainContainer className="flex flex-col items-center px-0">
    <div class="z-10 flex flex-col items-center gap-6 px-6 pt-20 pb-32">
      <h1
        class="max-w-[20ch] text-center text-[52px] leading-[1] font-black tracking-tight text-balance uppercase sm:text-6xl"
      >
        Donde la Tecnología, la Robótica y la Música se Encuentran
      </h1>
      <p class="text-light-gray max-w-md text-center">
        Proyecto musical que fusiona percusión y robótica para explorar nuevas formas de expresión.
      </p>
      <a
        href="#sobre-nosotros"
        class="bg-secondary text-background hover:bg-secondary-dark flex w-fit items-center gap-1 rounded-md px-3 py-2.5 font-semibold transition-colors"
      >
        <Icon name="mdi:arrow-bottom" class="size-6" /> Descubrí más <Icon name="mdi:arrow-bottom" class="size-6" />
      </a>
    </div>
    <div class="z-0 -mt-56 h-[375px] w-full lg:w-[70%] [&>astro-island>div]:h-full">
      <SlideInFromBottom client:load>
        <Image src={HeroImage} priority={true} alt="Hero" class="h-full w-full object-cover object-top" />
      </SlideInFromBottom>
    </div>
  </MainContainer>
</div>
